CREATE TABLE cursos (
                                        `id` INT NOT NULL auto_increment ,
                                        `name` VARCHAR(200) NOT NULL,
                                        `categoria` VARCHAR(100) NULL,
                                        PRIMARY KEY (`id`));
